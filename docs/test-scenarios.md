# 수동 테스트 시나리오

> **2026-08-11 갱신**: "10. 지원지역 확대 Batch 1+2"의 "27곳"/"26곳" 등은 2026-08-07 시점 기록으로
> 보존한다. 로컬 DB는 이후 **전국 SIGUNGU 255/255**로 완전히 확장됐다 — 유사지역 비교 기대값도
> "254곳까지"로 갱신해서 읽어야 한다. Demand/Spend 축 정규화가 log1p로 바뀐 점도 참고
> (`docs/scoring-model.md`).

이 세션(2026-07-21~22)에 고친/추가한 기능을 직접 눌러보면서 확인하기 위한 체크리스트다. 운영 배포
(https://tour-dna.lib.lc) 또는 로컬(`npm run dev` → http://localhost:3000)에서 위에서부터 순서대로
진행하면 된다. 각 항목은 "무엇을 하고 → 무엇이 보여야 정상인지"로 적었다.

## 2026-08-26 공모전 심사위원 관점 통합 QA 결과

로컬 `http://localhost:3000`에서 신규 프로젝트 생성으로 시작하는 경주 문화·역사 시나리오와
대전 축제 Anchor·경주 PET/ACCESSIBILITY 시나리오를 처음 화면부터 인쇄까지 실제로 확인했다.

- [x] 홈에서 10초 안에 `새 관광상품 기획`과 `데모 프로젝트 열기`를 찾을 수 있고, 3단계 제품
      흐름(조건 입력 → DNA 진단/전략 비교 → 실행안 출력)을 이해할 수 있다.
- [x] 신규 프로젝트에서 지역·여행월·역할·목표·기간·이동수단·테마·여행 조건을 입력하고 분석을
      생성했다. 분석 화면에서 DNA 5축, 최신 데이터 비율, 근거 보기, 전략 3안, 역할·테마 반영을
      확인했다.
- [x] 전략 선택 후 자동 초안의 유형별 기본 체류시간, 추천 후보의 거리·전략 적합도, 후보 추가·삭제,
      같은 날/다른 날짜 Drag & Drop, 지도 동선, 실시간 품질 패널을 확인했다.
- [x] `CLEAN → DIRTY → SAVING → SAVED → CLEAN` 저장 상태와 저장 후 재로드 데이터 일치를 확인했다.
- [x] 축제 후보 선택 → 연계 날짜·일차·기획 시간대 지정 → Anchor 확정, 대전 Anchor 코스 고정 →
      저장·재로드 → 행사 전/식사/행사 후/숙박 후보 재계산 흐름을 확인했다.
- [x] 대전 Anchor QA fixture의 공식 `searchFestival2` 사실을 재확인해 `contentId=4098134`의
      `유성온날`, `2026-08-14~2026-12-11`, 유성구 주소·국내 좌표·`sourceSnapshot`을 동일하게
      맞췄고, local setup을 연속 두 번 실행해 중복 Anchor·반복 갱신이 없는지 확인했다.
- [x] 후보 조사는 대전·세종·제천 대표 Anchor의 지역·여행월 조회 3회로 한정했고, 공식 상세 API나
      전국 행사 목록을 무작정 조회하지 않았다.
- [x] PET·ACCESSIBILITY의 공식 확인·조건부·미확인 상태와 상세 evidence를 확인했다. 미확인은
      이용 불가로 표시되지 않았고, 상세 영역을 접고 펼쳐도 저장 상태가 변하지 않았다.
- [x] 인쇄 화면에 기획 배경·핵심 지표·DNA·전략·코스·체크리스트·위험·KPI·데이터 근거가 포함됐다.
- [x] 자동 Playwright에서 390×844 overflow 없음, advisory 접기/펼치기, 버튼 fallback, reduced-motion을
      확인했다. 수동 인앱 브라우저는 고정 뷰포트라 390px 직접 설정은 자동 검증으로 대체했다.

판정은 P0 없음, 제품 코드 기준 즉시 수정 P1 없음, 정보 밀도·Anchor 저장 전 후보 즉시 갱신은
P2/POLISH 후속 과제다. 기존 demo fixture의 행사명·행사일 불일치는 공식 후보 스냅샷으로 교정했다.
전체 회귀는 `31 passed / 2 skipped`이며 skip은 stale/orphan Anchor fixture 부재에 따른 기존 선택적
skip이다.

> **0~6번은 현재 운영 배포에도 적용된 기능이다.** "7. 홍보자료(Phase 5)" 절은 2026-07-26 기준 로컬
> 구현·자동 테스트만 끝났고, `SelectedPlan.promoContent` migration이 원격 DB에 적용되지 않아 **운영
> 배포·로컬 개발 DB 어디서도 아직 실제로 눌러볼 수 없다** — migration 적용 후에 진행한다. "8. 역할·
> 국적·테마·여행월 반영(Phase 4)" 절은 DB 스키마 변경이 없어 로컬 개발 DB(`npm run dev`)에서는 지금
> 바로 확인할 수 있지만, 이 문서 작성 시점에는 아직 실제 브라우저로 수행되지 않았다(로컬 자동
> 테스트만 완료).

## 0. 사이트 접근 게이트 (SITE_ACCESS_PASSWORD를 설정한 경우에만 해당)

- [ ] 시크릿/무개인정보 창으로 아무 페이지(`/`, `/projects/new` 등)에 접속 → `/login`으로 자동 이동하는지
      확인
- [ ] 틀린 비밀번호 입력 → "비밀번호가 올바르지 않습니다." 오류가 뜨고 페이지에 그대로 남는지 확인
- [ ] 맞는 비밀번호 입력 → 원래 가려던 페이지로 이동하는지 확인
- [ ] (2026-08-06 추가) `/login` 화면의 "홈으로 돌아가기" 링크 클릭 → `/`로 이동하는지 확인(뒤로가기 없이도 동작)
- [ ] (2026-08-06 추가) 틀린 비밀번호 오류가 뜬 뒤에도 "홈으로 돌아가기" 링크가 그대로 남아 있는지 확인
- [ ] (2026-08-06 추가) 비밀번호로 보호된 프로젝트 페이지 접속 → "홈으로 돌아가기" 링크 클릭 → `/`로 이동하는지 확인
- [ ] (2026-08-06 추가) 헤더의 "프로젝트 목록" 메뉴가 삭제됐는지 확인(홈이 곧 목록이라 로고 링크와 중복이었음)
- [ ] (2026-08-06 헤더 잠금 버튼 제거로 이 항목은 브라우저 UI로 재현할 수 없다 — 대신 개발자 도구에서
      사이트 접근 쿠키를 지운 뒤 다시 아무 페이지나 들어가면 `/login`을 요구하는지로 대체 확인한다)
      쿠키 삭제 → 아무 페이지나 접속 → `/login`으로 이동하고, 다시 로그인을 요구하는지 확인
- [ ] (설정 안 했다면) 위 항목은 전부 건너뛰고 그냥 바로 들어가지는지 확인 — 게이트가 없으면 이게 정상

## 1. 새 프로젝트 생성 화면 (`/projects/new`)

- [ ] 시/도에서 **대전광역시** 선택 → 시/군/구 드롭다운에 "대전광역시"가 또 나오지 않고
      **동구/중구/서구/유성구/대덕구** 5개 자치구만 정상적으로 나오는지 확인(2026-08-13: `Region.name`
      오기입을 바로잡아 legacy "대전광역시 (DNA 지표는 유성구 기준)" 표시가 사라지고 실제 "유성구"로
      나온다 — 예전엔 시/도명이 그대로 중복 표시되던 버그였음)
- [ ] 시/도에서 **강원특별자치도** 선택 → 시/군/구에 **강릉시/양양군**이 나오는지 확인(2026-07-21에
      추가된 지역)
- [ ] 시/도를 바꾸면 프로젝트명 제안이 즉시 바뀌는지 확인(예: "대전광역시 9월 소규모 여행 기획" →
      "강원특별자치도 9월 소규모 여행 기획"), 프로젝트명을 직접 타이핑하면 그 이후로는 자동 제안이
      끊기고 입력한 값이 유지되는지 확인
- [ ] "분석 시작" 버튼에 마우스를 올리면 커서가 손가락(포인터) 모양으로 바뀌는지 확인(전체 페이지의
      다른 버튼들 — 실행안의 위/아래/삭제, 인쇄, 오류 화면 "다시 시도" 등도 동일하게 확인 가능)

## 2. 관광 DNA 분석 화면 (`/projects/[id]/analysis`)

- [ ] 대전광역시로 프로젝트를 만들어 분석 실행 → 수요/체류/소비/다양성/연계 5축 점수가 전부 정확히
      0점 또는 100점만 나오지는 않는지 확인(2026-07-21에 비교 지역을 3개→7개로, 2026-08-07에
      7개→27개로 늘려 완화한 부분 — 완전히 없어지지는 않으므로 일부는 여전히 0/100일 수 있음,
      "완화"이지 "해결"은 아님. 아래 10번 절 참고)
- [ ] "근거 보기"를 눌러 각 지표의 원값/정규화값/단위/기준월/출처/반영 규칙이 보이는지 확인
- [ ] 전략 3안이 서로 다른 점수/순위/근거 문구를 갖는지 확인

## 3. 전략 선택 → 실행안 (`/projects/[id]/plan`)

- [ ] 1순위가 아닌 다른 전략을 선택 → 상품명/콘셉트 문구/코스가 실제로 바뀌는지 확인(예전엔 전략을
      바꿔도 실행안이 그대로였던 버그였음)
- [ ] 코스의 장소들이 지리적으로 말이 되는 순서로 배치되는지 확인(예: 같은 지역 안에서 왔다갔다 하지
      않고 자연스럽게 이어지는지) — 이동 텍스트에 "이동 약 N분(약 X.Xkm, OO 기준)" 형태로 실제 거리
      기반 추정치가 나오는지 확인

### 3-1. 코스 편집 — 순서 변경

- [ ] 장소 옆 ↑/↓ 버튼으로 순서를 바꿔본다 → 이동 텍스트(travel)는 새로 이웃하게 된 장소 쌍 기준으로
      다시 계산되지만, **각 장소가 가진 시간(왼쪽의 시간 입력창)은 그대로 유지**되는지 확인(순서와
      시간이 분리되어 있음 — 예전에는 시간이 자리에 묶여 있었다)

### 3-2. 코스 편집 — 시간 직접 수정

- [ ] 아무 장소의 시간 입력창(⏰)을 클릭해 다른 시각으로 바꿔본다 → 바로 반영되는지 확인
- [ ] 일부러 **시간이 거꾸로 가게** 만들어본다(예: 뒤 순서 장소의 시간을 앞 장소보다 이르게 설정) →
      해당 장소 칸이 **빨간 테두리/배경**으로 바뀌고 "⚠ 이전 일정이 OO에 끝나는데 이 장소는 그 전에
      시작합니다" 같은 경고 문구가 뜨는지 확인
- [ ] 일부러 **이동시간보다 여유를 짧게** 만들어본다(예: 두 장소 시간 차이를 5분으로 줄이는데 실제
      필요한 이동시간은 그보다 김) → 마찬가지로 빨간 경고가 뜨는지 확인
- [ ] 시간을 다시 여유 있게 고치면 경고가 사라지는지 확인

### 3-2b. 코스 편집 — 체류시간 직접 수정

- [ ] 체류시간 숫자 입력(장소 유형별 기본값)을 다른 값(화면 입력은 10분 단위)으로 바꿔본다 → 바로
      반영되는지, 값을 늘려서 다음 장소와의 여유가 부족해지면 위 3-2의 빨간 경고가 뜨는지 확인

### 3-3. 코스 편집 — 삭제

- [ ] 아무 장소나 "삭제" 버튼 클릭 → 목록에서 바로 빠지는지 확인

### 3-4. 코스 편집 — 다른 날짜로 이동 (1박 2일 이상 프로젝트에서만 가능)

- [ ] 장소 옆 "N일차" 선택창에서 다른 날짜를 고른다 → 원래 날짜에서는 빠지고 고른 날짜에 나타나는지,
      **원래 갖고 있던 시간을 그대로 유지**하는지 확인(옮긴 날짜에 이미 비슷한 시간대 장소가 있으면
      위 3-2의 빨간 경고가 뜰 수 있음 — 정상 동작)

### 3-5. 코스 편집 — 장소 검색 후 추가

- [ ] 날짜 아래 "+ 장소 추가" 클릭 → 검색창이 열리는지 확인
- [ ] 장소 이름 일부를 입력 → 같은 지역에 등록된 장소 중 일치하는 것만 결과로 나오는지 확인(이미 코스에
      들어있는 장소는 검색 결과에서 자동으로 빠짐)
- [ ] 결과에서 "추가" 클릭 → 해당 날짜 맨 끝에 새 장소가 추가되고, 검색창이 자동으로 닫히는지 확인
- [ ] **한 날짜에 4곳을 이미 다 채운 상태에서도** "+ 장소 추가"가 계속 활성화되어 있고, 5번째·6번째도
      제한 없이 추가되는지 확인(예전엔 "이 날짜는 가득 찼습니다"로 막혀 있었음)

### 3-6. 운영 체크리스트/위험과 대응안/KPI 편집

- [ ] "운영 체크리스트" 아래 입력창에 텍스트를 넣고 "추가" 클릭 → 목록에 바로 추가되는지 확인, 항목별
      "삭제" 버튼으로 지울 수 있는지 확인(위험과 대응안·KPI도 각각 동일하게 추가/삭제 가능한지 확인)
- [ ] KPI 메모/메모 입력란은 그대로 있는지 확인(이번에 손대지 않은 부분)
- [ ] 저장 후 인쇄 화면(`/print`)에서 방금 추가/삭제한 체크리스트·위험·KPI 내용이 개수 제한 없이 전부
      반영돼 보이는지 확인(예전엔 체크리스트 5개까지만, 위험과 대응안은 아예 안 보였음)

### 3-7. 저장/이탈

- [ ] 최초 진입 시 "현재 저장된 내용과 같습니다"가 보이고, 실제 저장 전에는 "모든 변경사항이
      저장되었습니다"가 보이지 않는지 확인
- [ ] 상품명이나 코스를 수정하면 "저장하지 않은 변경사항이 있습니다"가 뜨는지 확인
- [ ] 저장 클릭 직후 "저장 중..."과 버튼 비활성화가 보이고, 실제 성공 응답 뒤에만 "모든 변경사항이
      저장되었습니다"로 바뀌는지 확인
- [ ] 저장 성공 후 다시 편집하면 성공 문구가 사라지고 "저장하지 않은 변경사항이 있습니다"로
      돌아가는지, 새로고침(F5) 후에는 CLEAN 문구로 시작하는지 확인
- [ ] 저장 실패 시 오류 안내와 미저장 변경 상태가 함께 남고, 저장 재시도 후 성공 문구로 바뀌는지
      확인
- [ ] 상세 접기/펼치기, 후보 날짜 임시 선택만으로 저장 상태가 바뀌지 않는지 확인
- [ ] 수정 후 저장하지 않고 브라우저 탭을 닫거나 새로고침하려 하면 브라우저 자체의 "변경사항이
      저장되지 않을 수 있습니다" 경고가 뜨는지 확인(단, 앱 내부 링크로 이동할 때는 이 경고가 뜨지
      않는 게 정상 — 알려진 제한사항)

## 4. 인쇄/PDF (`/projects/[id]/print`)

- [ ] 실행안 화면에서 "인쇄/PDF 보기" 클릭 → 화면 조작 버튼(위/아래/삭제/저장 등) 없이 깔끔한 인쇄용
      레이아웃만 보이는지 확인
- [ ] 화면 상단의 "← 실행안으로 돌아가기" 링크로 이전 화면으로 돌아갈 수 있는지 확인(예전엔 이 화면에
      진입하면 되돌아갈 방법이 없었음)
- [ ] 브라우저 인쇄 미리보기(Ctrl+P) 시 A4 1~2페이지 분량으로 나오는지 확인

## 5. 지도 (`/projects/[id]/analysis` 하단)

- [ ] 카카오맵 키가 정상 등록되어 있으면 실제 지도가, 없으면 좌표/주소 목록 fallback이 뜨는지 확인

### 5-1. 실행안 코스 지도 (`/projects/[id]/plan`, 2026-07-22 추가)

- [ ] "일자·시간대별 코스" 위쪽에 지도가 뜨고, 코스 장소들이 방문 순서대로 마커+선(동선)으로 이어지는지
      확인
- [ ] 1박 2일 이상 프로젝트에서는 "1일차/2일차" 탭이 뜨고, 탭을 누르면 해당 날짜 동선만 다시 그려지는지
      확인(1일짜리 프로젝트는 탭 없이 바로 지도만 보임)
- [ ] 마커를 클릭하면 "N. 시간 장소명" 정보창이 뜨는지 확인
- [ ] 코스에서 장소를 추가/삭제/다른 날짜로 이동하면 지도도 그에 맞춰 다시 그려지는지 확인
- [ ] 좌표 정보가 없는(예전에 저장된) 실행안이라면 "좌표 정보가 있는 장소가 없어 동선을 표시할 수
      없습니다" 안내가 뜨는지 확인(오류가 아니라 정상적인 안내임)
- [ ] 아직 지도에서 장소를 검색해 코스에 바로 추가하는 기능은 없다 — 검색은 여전히 3-5의 텍스트
      검색창에서만 가능함(알려진 제한사항)

## 6. 새 지역 추가 (운영자용, 코드 작업 필요)

- [ ] `npm run verify:region -- --name 대전_유성구 --area-cd 30 --signgu-cd 30200 --tour-api-area-code 3`
      실행 → PASS로 나오는지 확인(기존 확인된 지역으로 스크립트 동작만 검증하는 용도)
- [ ] 새 지역을 추가하려면 `docs/operator-checklist.md`의 "새 지역 추가 절차" 참고

## 7. 홍보자료 (Phase 5) — Production 브라우저 검증 완료(2026-08-01)

`20260726000000_add_selected_plan_promo_content` migration은 원격 Neon DB에 적용 완료됐고,
2026-08-01에 Production(`tour-dna.lib.lc`)에서 Playwright(실 Chromium)로 아래 항목을 전부 확인했다.
(참고: 무인 자동화 도구의 탭이 배경 상태면 hydration이 안 돼 오탐이 날 수 있다 —
`docs/operator-checklist.md`의 "무인 브라우저 자동화 도구의 배경 탭 오탐" 항목 참고.)

- [x] 홍보자료를 아직 생성하지 않은 프로젝트의 실행안 화면에서 "아직 생성된 홍보자료가 없습니다."와
      "홍보자료 생성" 버튼만 보이는지 확인
- [x] "홍보자료 생성" 클릭 → 제안서 요약(3문장)/랜딩페이지/Instagram/블로그/역할별 자료가 채워지는지 확인
      (서버 요청 정확히 1회 발생 확인)
- [x] 문구를 수정하면 "저장하지 않은 변경사항이 있습니다."로 바뀌는지 확인
- [x] "저장" 클릭 → "저장했습니다."로 바뀌고, 새로고침(F5) 후에도 수정한 내용이 그대로 남아있는지 확인
- [x] 이미 홍보자료가 있는 상태에서 "재생성" 클릭 → 확인창이 뜨고, 승인하면 "홍보자료를 다시
      생성했습니다."로 바뀌며 서버 요청이 정확히 1회만 발생하는지 확인
- [x] 제안서 요약·랜딩·Instagram·블로그·역할별 자료 각각의 "복사" 버튼과 "전체 복사" 버튼이 클립보드에
      정상 복사되는지 확인(전체 복사 결과에서 6개 채널 라벨이 정확히 한 번씩만 등장, 중복 헤더 없음)
- [x] 프로젝트 역할을 "여행사/DMC"로 만들면 상품명·타깃 고객·판매 포인트 3개·일정 하이라이트가, "지자체/
      관광재단"으로 만들면 보도자료 제목·리드·추진 배경·핵심 프로그램·데이터 기반 근거·기대 효과가
      보이는지 확인 — 채널 우선순위도 역할별로 다르게(`computeChannelPriority()` 규칙대로) 정렬됨을 확인
- [x] 모바일 해상도(375×812)에서도 홍보자료 섹션이 가로 스크롤 없이 정상 노출되는지 확인
- [ ] 모바일 폭(개발자도구 반응형 보기 등)에서 버튼과 입력창이 화면 밖으로 넘치지 않는지 확인
- [ ] "인쇄/PDF 보기"에서 저장된 홍보자료가 편집 버튼·입력창 없이 텍스트로만 출력되는지 확인(생성한
      적 없는 프로젝트는 홍보자료 섹션 자체가 인쇄되지 않아야 정상)

## 8. 역할·국적·테마·여행월 반영 (Phase 4, 2026-07-26 로컬 구현 — DB 스키마 변경 없음, 지금 바로 실행 가능)

DB migration이 필요 없어 로컬 개발 서버(`npm run dev`)에서 바로 확인할 수 있다. 자동 테스트
(`audienceContext.test.ts` 27개 + `strategy.test.ts`/`planBuilder.test.ts`/`planService.test.ts`의
Phase 4 관련 항목)로 로직 자체는 검증했지만, 실제 브라우저로 눌러본 적은 아직 없다.

- [ ] 같은 지역·같은 나머지 조건에서 역할만 "여행사/DMC" → "지자체/관광재단"으로 바꿔 두 프로젝트를
      만들고, `/projects/[id]/analysis`의 전략 카드에서 "역할 적합도" 점수와 총점, 순위 중 최소
      하나가 달라지는지 확인
- [ ] 위 두 프로젝트에서 "선정 이유" 두 번째 문장에 역할 라벨("여행사/DMC 관점" 또는 "지자체/관광재단
      관점")이 실제로 다르게 표시되는지 확인
- [ ] 같은 조건에서 국적만 "내국인" → "외국인"으로 바꿔 "운영 적합도(국적 반영)" 점수가 달라지는
      전략이 있는지 확인(문화·역사 체험형처럼 해설 의존도가 높은 템플릿에서 더 잘 드러남)
- [ ] "선호 테마"에 "미식"처럼 특정 키워드를 넣으면 "타깃 적합도(테마 반영)" 점수가 관련 전략(로컬미식·
      시장 연계형 등)에서 더 높게 나오는지 확인
- [ ] 여행월을 장마철(6~7월)로 설정하고 실행안 화면의 "위험 요인"에 우천 관련 안내가 추가로 보이는지
      확인(실내 위주 전략에서는 추가되지 않아야 함)
- [ ] "입력 조건 요약"에 선호/제외 테마가 표시되고, 하단에 "역할·국적·테마·여행월은 지역의 객관적
      관광 DNA를 바꾸지 않는다"는 안내 문구가 보이는지 확인
- [ ] 같은 조건으로 두 번 분석을 재실행해도(동일 입력) 전략 점수·순위·근거가 완전히 동일한지 확인(결정론성)
- [ ] 인쇄 화면 상단에 역할 라벨이 함께 표시되는지 확인

## 9. 대표 시나리오 3개 (P0-2, 2026-07-27 로컬 구현 — DB 스키마 변경 없음, 지금 바로 실행 가능)

`/projects/new` 상단의 "대표 시나리오로 빠르게 시작" 카드 3개는 강릉/경주/제천의 실제 지역 데이터로
분석 조건별 차이를 시연하기 위한 프리셋이다. 단위 테스트(`contestScenarios.test.ts` 24개)로 실제 계산
결과(DNA·전략 점수·순위·근거·KPI·체크리스트·위험요인)가 세 시나리오마다 다름을 확인했지만, 카드 클릭
→ 프로젝트 생성 → 분석까지 실제 브라우저로 눌러본 적은 아직 없다.

- [ ] `/projects/new`에서 카드 3개("강릉 여름 미식·자연 상품", "경주 가을 문화·역사 전략", "제천 겨울
      웰니스 상품")가 지역·역할·타깃·테마·여행월·기획 의도와 함께 보이는지 확인
- [ ] 카드를 클릭하면 시·도/시·군·구/역할/내외국인/여행월/선호 테마 입력란이 해당 시나리오 값으로
      즉시 바뀌는지 확인(예: 경주 카드 → 시·도 "경상북도", 시·군·구 "경주시", 역할 "지자체/관광재단",
      내/외국인 "내국인", 선호 테마 "문화, 역사", 여행 월 "10월")
- [ ] 카드 적용 후에도 아무 입력란이나 자유롭게 다시 수정할 수 있는지 확인(예: 여행 월만 다른 달로
      바꿔도 나머지 값은 유지되는지)
- [ ] 카드를 고르지 않고 처음부터 직접 입력해도 기존과 동일하게 "분석 시작" 버튼으로 정상 진행되는지
      확인(프리셋 없는 기존 흐름 회귀 확인)
- [ ] 강릉(여행사)과 경주(지자체) 두 프로젝트를 각각 만들어 분석 화면에서 전략 점수·순위·근거·KPI·
      체크리스트·위험요인 중 최소 일부가 실제로 다르게 나오는지 확인(두 지역 모두 1위 전략이 "야간·
      체류 확대형"으로 우연히 같게 나올 수 있으나, 점수·역할 적합도·근거 문구·KPI는 달라야 정상이다)
- [ ] 제천(겨울) 프로젝트의 실행안 화면에서 한파·결빙 관련 위험 요인이 보이는지 확인
- [ ] 강릉·경주는 로컬에 등록된 POI fixture가 없어(제천만 있음) 관광 DNA의 "공급(Network)" 축이
      데이터 부족으로 표시될 수 있다 — 실제 TourAPI 키가 동작 중이면 라이브 조회로 채워질 수도 있으니,
      두 경우 모두 화면에 오류 없이 안전하게 표시되는지 확인
- [ ] 모바일 폭(개발자도구 반응형 보기)에서 카드 3개가 세로로 쌓이고 텍스트가 화면 밖으로 넘치지
      않는지 확인

---

각 항목에서 예상과 다르게 동작하면, 어떤 단계에서 어떻게 달랐는지(스크린샷이 있으면 더 좋음) 알려주면
바로 원인을 찾아 고칠 수 있다.

## 10. 지원지역 확대 Batch 1+2 (2026-08-07, 지원 SIGUNGU 7→27개)

기존 7개 지역에 20개 지역을 추가한 뒤 확인해야 할 항목이다. 상세 지역 목록·데이터 품질은
`docs/data-dictionary.md`, 발견된 버그·운영 사고는 `docs/operator-checklist.md` 참고.

- [ ] `/projects/new`의 시/도 드롭다운에 새로 추가된 광역(경기도·충청남도·부산광역시·대구광역시)이
      나오고, 각 시/군/구 드롭다운에 신규 지역이 나오는지 확인(예: 강원특별자치도 → 화천군/삼척시/
      정선군/평창군)
- [ ] 신규 지역(예: 안동시) 프로젝트 생성 → 분석 실행 → DNA 5축 점수가 정상 계산되는지 확인
- [ ] 신규 지역의 "근거 보기"에서 관광자원수요(문화자원수요) 지표가 보이지 않는 것이 정상임을 확인
      (기존 7곳과 달리 ESTIMATED fixture가 없어 이 지표 자체가 근거 목록에 없다 — 결측 오류가 아님)
- [ ] 신규 지역으로 분석 시 유사지역 비교에 "현재 지원지역 27곳 중 26곳을 비교했습니다" 문구가
      뜨는지 확인(소규모 모집단 경고가 더 이상 뜨지 않아야 정상 — **2026-08-11 갱신**: 로컬 DB는
      전국 SIGUNGU 255개로 확장돼 실제 문구는 "255곳 중 254곳"이 된다. 이 항목은 2026-08-07 Batch
      1+2(27개) 시점 기대값이므로 지금 재현하려면 숫자를 255/254로 읽어야 한다)
- [ ] 신규 지역 전략 선택 → 실행안 생성 → POI 좌표가 전부 유효하고 지도가 정상 렌더링되는지 확인
      (해양/역사/산악/도심/휴양/미식 유형 중 하나씩 확인 권장 — 해운대구/안동시/정선군/대구 중구/
      화천군/공주시가 대표 예시)
- [ ] 대구 중구로 프로젝트를 만들어 POI가 실제로 수집되는지 확인(주소 필터 override 회귀 확인 —
      `tests/unit/syncService.test.ts`의 자동 테스트로도 확인되지만, 실 데이터 기준 재확인 목적)
- [ ] 기존 7개 지역(제천·강릉·경주·제주·통영·양양·대전) 프로젝트를 재분석했을 때, 코호트가
      27개로 늘어난 만큼 축 점수가 예전과 달라질 수 있음을 확인(오류가 아니라 정상적인 상대평가
      변화) — 기존에 저장된(재분석하지 않은) `AnalysisResult`는 그대로 유지되는지도 함께 확인
- [ ] `npm run sync:tourism-data`를 인자 없이/위치 인자로 실행해 baseYm이 올바르게 적용되는지 확인
      (`--base-ym=` 같은 플래그 형식은 지원하지 않으며, 잘못 넘기면 전체 동기화가 실패한다 — 실제
      발생했던 사고)
